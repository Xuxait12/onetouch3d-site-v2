

## Plan: Mobile card layout adjustments on Home page

Two class changes in `src/pages/Home.tsx`:

1. **Line ~79** — Grid classes: change `grid-cols-1` to `grid-cols-2`
2. **Line ~95** — Image aspect: change `aspect-[5/4]` to `aspect-[3/4]`

No other changes.

